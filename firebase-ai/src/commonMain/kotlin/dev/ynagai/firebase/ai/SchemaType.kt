package dev.ynagai.firebase.ai

/**
 * Defines the data types available for [Schema] definitions.
 */
enum class SchemaType {
    STRING,
    INTEGER,
    NUMBER,
    BOOLEAN,
    ARRAY,
    OBJECT,
}
